export type ParallaxImageData = {
    title: string
    imageUrl: string
    key: {
        current: string
        _type: 'slug'
    }
}

export type PreparationItemData = {
    title: string
    description: any
    mitigations: string[]
}
